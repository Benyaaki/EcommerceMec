{
  "name": "autorepuestos-chile",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && npx gh-pages -d dist"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  },
  "dependencies": {
    "cobe": "^2.0.1"
  }
}
